#include <iostream>

using namespace std;

int t;

int main() {
    cin.tie(0);
    cout.tie(0);
    ios_base::sync_with_stdio(0);
    
    int a, b;
    cin >> a >> b;
    cout << 2*b-a;
}